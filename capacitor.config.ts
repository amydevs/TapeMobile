import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Aeriform.Tape',
  appName: 'Tape',
  webDir: 'app',
  bundledWebRuntime: false
};

export default config;
